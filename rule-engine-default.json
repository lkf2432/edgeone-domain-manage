{
  "FormatVersion": "1.0",
  "Rules": [
    {
      "RuleName": "请修改为规则名称（如 dp、cache-rule 等）",
      "Branches": [
        {
          "Condition": "${http.request.host} in ['修改为需要加速的域名']",
          "Actions": [
            {
              "Name": "WebSocket",
              "WebSocketParameters": {
                "Switch": "on",
                "Timeout": 300
              }
            }
          ],
          "SubRules": []
        }
      ],
      "Description": []
    }
  ]
}
